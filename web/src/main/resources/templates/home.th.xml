<thlogic>
    <attr sel="#headid"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#play"
          th:href="play"
          th:text="#{game.play}"/>
    <attr sel="#footerid"
          th:replace="~{fragments/footer :: footFragment}"/>
    <attr sel="#home" th:text="#{game.home}"/>
    <attr sel="#welcome" th:text="|#{game.welcome} Guess the Number Game|"/>
</thlogic>